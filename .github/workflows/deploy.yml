name: Deploy to Vercel

# Legacy duplicate of vercel-deploy.yml. Keep disabled so attestation
# upserts and Track Deployments do not flood the Actions queue.
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Use workflow Vercel Deploy (vercel-deploy.yml) instead."
