# ... existing content ...

# Cloudflare
.registry-cid
wrangler.toml.local
.wrangler/

# IPFS uploads
public/ipfs-asset-map.json

# API backup directory (temporary build artifact)
api-backup/
node_modules

# Next.js build output
.next/
out/

# Environment variables (contains sensitive data like private keys)
.env
.env.local
.env*.local
.vercel

# Local content/build caches
kasparex-hub/.cache/
