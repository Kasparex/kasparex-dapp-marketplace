# ... existing content ...

# Cloudflare
.registry-cid
wrangler.toml.local
.wrangler/

# IPFS uploads
public/ipfs-asset-map.json

# API backup directory (temporary build artifact)
api-backup/
node_modules
