# ... existing content ...

# Cloudflare
.registry-cid
wrangler.toml.local
.wrangler/
workers/verify.json
workers/enroll.json

# IPFS uploads
public/ipfs-asset-map.json

# API backup directory (temporary build artifact)
api-backup/
node_modules

# Hardhat / TypeScript build artifacts
artifacts/
cache/
tsconfig.tsbuildinfo

# Next.js build output
.next/
out/

# Local tooling (silverc build tree)
.tools/

# Environment variables (contains sensitive data like private keys)
.env
.env.local
.env*.local
.vercel
