// Crowdfund goal verification reference.

pragma silverscript ^0.1.0;

contract KasparexCrowdfund() {
    #[covenant(binding = cov, from = 1, to = 8, mode = verification)]
    function verify_goal(int goal_sompi, int raised_sompi) {
        require(raised_sompi >= goal_sompi);
    }
}
