/** UTF-8 prefix on L1 tx payload for Chronicles vault unlocks (hex in `sendKaspa` options.payload). */
export const VAULT_PAYLOAD_PREFIX = 'kxv1:';

/** Max combined KREX + NFT discount (percent) applied client-side; server accepts payments down to this floor. */
export const VAULT_MAX_COMBINED_DISCOUNT_PERCENT = 28;
