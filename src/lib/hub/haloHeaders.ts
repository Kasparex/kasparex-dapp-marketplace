/** Tailwind class: show hub halo hero blocks on desktop (lg+) only. */
export const HUB_HALO_DESKTOP_ONLY = 'hidden lg:block';

/** Tailwind class: mobile-only halo fallback (disabled; content starts at filters). */
export const HUB_HALO_MOBILE_FALLBACK = 'hidden';
