/** Shared layout for `/nodes` dashboard cards and secondary tabs (full width within `max-w-7xl` parent). */
export const NODES_DASH_CARD =
  'rounded-xl border border-zinc-200 dark:border-zinc-800 overflow-hidden bg-white dark:bg-zinc-900 p-6';

/** Vertical stack for tab bodies - mirrors dashboard `space-y-6` rhythm, no inner max-width cap. */
export const NODES_TAB_STACK = 'space-y-6 w-full min-w-0';
