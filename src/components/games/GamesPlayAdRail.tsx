'use client';

import { PanelAdSlider } from '@/components/ads/PanelAdSlider';

/**
 * Right-column ads on game dashboards (same unwrapped PanelAdSlider pattern as Tokens).
 */
export function GamesPlayAdRail() {
  return <PanelAdSlider slotId="HALO_GAMES_RIGHT" id="ad-slot-games-play-rail" />;
}
